import 'package:flutter/material.dart';
// all the colors used are assigned to variables

//primary button color
const Color btnPrimary = Color(0xFF0FA3E2);



//login page button's text colors
const Color textPrimary = Color(0xFF000000);
